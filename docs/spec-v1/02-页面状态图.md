# 棋者弈也 V1 页面状态图

本文件描述客户端页面状态。页面状态与服务端房间状态相关，但不是一一对应：例如聊天抽屉、分享面板和缩放是客户端局部状态。

---

## 1. 全局应用状态

```mermaid
stateDiagram-v2
    [*] --> Booting
    Booting --> HomeReady: 前端初始化完成
    Booting --> FatalError: 静态资源初始化失败

    HomeReady --> Connecting: 创建或加入棋局
    Connecting --> WakingServer: 首次连接超过阈值
    WakingServer --> JoiningRoom: Socket 连接成功
    Connecting --> JoiningRoom: Socket 连接成功
    JoiningRoom --> RoomReady: 房间快照成功
    JoiningRoom --> RoomNotFound: ROOM_NOT_FOUND
    JoiningRoom --> CapacityError: 观众席已满
    JoiningRoom --> IdentityError: token 无效

    RoomReady --> Reconnecting: Socket 断开
    Reconnecting --> RoomReady: 重连和同步成功
    Reconnecting --> WakingServer: 长时间未连接
    Reconnecting --> RoomNotFound: 服务重启或房间失效

    RoomReady --> RoomClosed: ROOM_CLOSED
    RoomClosed --> HomeReady: 返回首页
    RoomNotFound --> HomeReady: 返回首页
    CapacityError --> HomeReady: 返回首页
    IdentityError --> JoiningRoom: 以新观众身份加入
```

### 全局显示规则

| 状态 | 主文案 | 允许操作 |
|---|---|---|
| `Booting` | 正在加载棋者弈也 | 无 |
| `Connecting` | 正在连接游戏服务器 | 返回首页 |
| `WakingServer` | 游戏服务器正在准备中，请保持页面打开 | 继续等待、重新连接、返回首页 |
| `JoiningRoom` | 正在进入棋局 | 返回首页 |
| `Reconnecting` | 连接中断，正在恢复棋局 | 返回首页 |
| `RoomNotFound` | 棋局不存在或已经失效 | 创建新棋局、返回首页 |
| `RoomClosed` | 双方已离开，本棋局已经关闭 | 创建新棋局、返回首页 |

---

## 2. 首页状态

```mermaid
stateDiagram-v2
    [*] --> Idle
    Idle --> NicknameInvalid: 昵称为空或超长
    NicknameInvalid --> Idle: 修改昵称
    Idle --> Creating: 点击创建棋局
    Creating --> Created: 服务端创建成功
    Creating --> CreateFailed: 网络或服务错误
    CreateFailed --> Creating: 重试

    Idle --> JoinForm: 点击加入棋局
    JoinForm --> Joining: 输入房间号或打开邀请链接
    Joining --> Joined: 加入成功
    Joining --> JoinFailed: 房间不存在/观众已满/网络错误
    JoinFailed --> JoinForm: 修改或重试
```

### 首页布局

```text
棋者弈也
好友五子棋 · ONLINE GOMOKU
无需注册，邀请好友即刻对弈

[昵称________________]
[创建棋局]

房间号 [______] [加入棋局]
```

---

## 3. 房间页面主状态

```mermaid
stateDiagram-v2
    [*] --> WaitingOpponent
    WaitingOpponent --> StartingInitial: 第二名棋手加入
    StartingInitial --> Playing: 到达 startsAt

    Playing --> UndoPending: 棋手申请悔棋
    UndoPending --> Playing: 同意并撤回 / 拒绝 / 取消

    Playing --> Paused: 任一棋手断线或明确离开
    Paused --> Playing: 双方恢复在线

    Playing --> FinishedWin: 五子及以上
    Playing --> FinishedDraw: 满盘

    FinishedWin --> RematchWaiting: 一方请求再战
    FinishedDraw --> RematchWaiting: 一方请求再战
    RematchWaiting --> StartingSwap: 双方确认
    StartingSwap --> Playing: 到达 startsAt

    WaitingOpponent --> Closed: 双方离开或结束房间
    Playing --> Closed: 双方离开或结束房间
    Paused --> Closed: 双方离开或结束房间
    FinishedWin --> Closed: 双方离开或结束房间
    FinishedDraw --> Closed: 双方离开或结束房间
```

### 棋手页面状态

| 服务端状态 | 棋手主提示 | 棋盘操作 |
|---|---|---|
| `WAITING` | 等待好友加入 | 禁用 |
| `STARTING(initial_random)` | 正在随机分配黑白 | 禁用 |
| `STARTING(rematch_swap)` | 双方已交换黑白 | 禁用 |
| `PLAYING` 且轮到自己 | 轮到你 | 启用 |
| `PLAYING` 且轮到对方 | 等待对方落子 | 禁用 |
| `PAUSED` | 对方暂时离线，棋局已暂停 | 禁用 |
| `FINISHED(win)` | 黑胜或白胜 | 仅浏览 |
| `FINISHED(draw)` | 和棋 | 仅浏览 |
| `CLOSED` | 棋局已经关闭 | 禁用 |

### 观众页面状态

| 服务端状态 | 观众主提示 |
|---|---|
| `WAITING` | 等待第二名棋手加入 |
| `STARTING` | 正在分配或交换黑白 |
| `PLAYING` | 黑/白棋回合 |
| `PAUSED` | 棋手暂时离线，棋局暂停 |
| `FINISHED` | 黑胜、白胜或和棋 |
| `CLOSED` | 棋局已经关闭 |

观众页面始终不显示落子、悔棋和再战操作。

---

## 4. 手机端对局界面状态

```mermaid
stateDiagram-v2
    [*] --> NoCandidate
    NoCandidate --> CandidateSelected: 轻点合法交叉点
    CandidateSelected --> NoCandidate: 取消选择
    CandidateSelected --> Submitting: 确认按钮或再次点击同一点
    Submitting --> NoCandidate: 服务端成功
    Submitting --> CandidateSelected: 服务端拒绝，可修正

    NoCandidate --> Panning: 单指移动超过阈值
    CandidateSelected --> Panning: 单指移动超过阈值并取消候选
    Panning --> NoCandidate: 手势结束

    NoCandidate --> Pinching: 双指开始
    CandidateSelected --> Pinching: 双指开始并取消候选
    Pinching --> NoCandidate: 双指结束

    NoCandidate --> ChatOpen: 打开聊天
    CandidateSelected --> ChatOpen: 打开聊天并保留候选
    ChatOpen --> NoCandidate: 关闭聊天且无候选
    ChatOpen --> CandidateSelected: 关闭聊天且候选仍在
```

### 顶部固定栏

```text
● 你执黑 ｜ 轮到你 ｜ 对手在线
确认落子：开
```

### 底部固定栏

```text
有候选：取消选择 ｜ 确认落子 ｜ 聊天 2 ｜ 更多
无候选：确认：开 ｜ 悔棋 ｜ 聊天 2 ｜ 更多
观众：  聊天 2 ｜ 分享房间 ｜ 棋盘居中 ｜ 更多
```

---

## 5. 覆盖层与抽屉状态

同一时间只允许一个主要覆盖层：

- `ShareSheet`
- `ChatDrawer`
- `MoreMenu`
- `UndoDialog`
- `LeaveConfirm`
- `ResultSheet`

优先级：

1. 连接或房间关闭错误。
2. 离开确认。
3. 悔棋请求。
4. 胜负结果。
5. 聊天、分享和更多菜单。

胜负发生时：

```text
获胜连线高亮
→ 印章出现
→ 600ms 后 ResultSheet 展开
```

---

## 6. 页面状态与 URL

| URL | 页面 |
|---|---|
| `/` | 首页 |
| `/room/:roomId` | 房间页；先连接后端再加入或恢复身份 |

必须监听浏览器 `popstate`：

- 从房间返回首页时更新 React 状态。
- 前进回房间时重新执行加入或恢复流程。
- 直接刷新 `/room/:roomId` 时由静态站点 rewrite 到 `/index.html`，客户端再解析房间号。
